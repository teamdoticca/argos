# Status

planned

Packaging epic: make `Argos` a consumable NuGet with native RID assets and reproducible pack/CI.
Depends on [m13-nuget-wrapper](../../done/m13-nuget-wrapper/) (wrapper already shipped).
Implementation deferred to follow-up briefs 01→05 in order.
