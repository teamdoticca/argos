# Design — Filesystem Semantics

## Contract alignment

All observable outputs must derive from `WorkspaceSnapshot` (or be an implementation detail that never becomes public truth).

## Notes

See `docs/architecture.md` for the frozen system contract.
