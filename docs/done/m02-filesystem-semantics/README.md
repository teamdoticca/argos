# Filesystem Semantics

**Epic:** `m02-filesystem-semantics`
**Status:** see `status.md`

## Goal

PathIdentity, symlinks, junctions, mounts, case sensitivity, and canonicalization.

## Acceptance criteria

- Design documented in `design.md`
- Implementation matches the frozen architecture contract
- `docs/roadmap.md` and `docs/execution-plan.md` updated on progress
- On completion, move this folder to `docs/done/m02-filesystem-semantics` and sync roadmap + execution plan

## Out of scope

Anything not required by the goal above. See root Non-goals.
