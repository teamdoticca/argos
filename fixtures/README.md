# Fixture README

Synthetic workspaces used by tests and `argos-bench`.

| Fixture | Description |
|---------|-------------|
| `small-pnpm` | Two-package pnpm workspace (`app`, `lib`) |
| `nested-npm` | Nested `package.json` under `src/web` with no root workspace manifest |
| `dotnet-sln` | Minimal `.sln` + `.csproj` tree (`src/App`) |
| `docs-handbook` | Planning tree named `handbook/` (not `docs/`) with roadmap + architecture |
| `docs-guidance-odd` | `AGENTS.md` points at `knowledge-base/` + `.cursor/rules` |
